package Day56MySQL4;

public class 列类型 {
	/*
	 * 分类（详细见“MySQL列类型（数据类型）”思维导图）：
	 *     （1）数值类型
	 *     （2）文本·二进制类型
	 *     （3）时间日期
	 *     
	 * 数值型（整数）的基本使用
	 *     1.说明使用规范：在能够满足需求的情况下，尽量选择占用空间小的类型（如果不确定范围，则一般选择int类型）
	 *     
	 * 定义一个无符号的整数：
	 *     create table t01(id tinyint unsigned);无符号（范围：0~255）
	 *     create table t02(id tinyint);有符号（范围：-128~127）
	 */
}
