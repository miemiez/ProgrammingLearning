const h1 = <h1>Hello world</h1>;
/*
在上面的示例中，我们使用 JSX 创建了一个 <h1> 元素，并将其赋值给变量 element。
这个 JSX 元素将会被转换为一个 React 元素，并最终渲染为对应的 HTML <h1> 元素。

JSX 具有以下特点：
    (1)使用类似 HTML 的语法，使界面代码更直观易读。
    (2)可以使用 JavaScript 表达式嵌入到 JSX 中，以动态生成内容。
    (3)支持组件的嵌套和组合，使得构建复杂的用户界面变得简单。

需要注意的是，JSX 并不是原生的 JavaScript 语法，而是通过编译工具（如 Babel）
将 JSX 转换为有效的 JavaScript 代码，以便在浏览器中执行。
*/