1.变量在程序中保存可重用的数据并将其与名称相关联。
2.变量存储在内存中。
3.该var关键字用于 ES6 之前的 JS 版本。
4.let是可以重新分配变量时声明变量的首选方式，并且const是声明具有常量值的变量的首选方式。
5.未初始化的变量存储原始数据类型undefined。
6.数学赋值运算符使计算新值并将其分配给同一变量变得容易。
7.该+运算符用于连接字符串，包括保存在变量中的字符串值。
8.在 ES6 中，模板文字使用反引号`并将${}值插入到字符串中。
9.关键字typeof返回值的数据类型（作为字符串）。