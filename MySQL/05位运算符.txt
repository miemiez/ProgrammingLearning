(1) 按位与（&）：对两个整数进行按位与操作，返回一个新的整数，该整数的每个位都是两个操作数相应位的逻辑与结果。

(2) 按位或（|）：对两个整数进行按位或操作，返回一个新的整数，该整数的每个位都是两个操作数相应位的逻辑或结果。

(3) 按位异或（^）：对两个整数进行按位异或操作，返回一个新的整数，该整数的每个位都是两个操作数相应位的逻辑异或结果。

(4) 按位取反（~）：对一个整数进行按位取反操作，返回一个新的整数，该整数的每个位都是操作数相应位的逻辑取反结果。